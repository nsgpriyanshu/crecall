import { FaceIcon, ImageIcon, SunIcon } from '@radix-ui/react-icons'

function icons() {
  return (
    <div>
      <FaceIcon />
      <SunIcon />
      <ImageIcon />
    </div>
  )
}

export default icons
